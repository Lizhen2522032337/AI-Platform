{
  "version": "1",
  "tables": [
    {
      "schema": "示例SCHEMA",
      "name": "示例表名",
      "description": "说明这张表代表的业务对象、数据粒度和更新时间",
      "columns": [
        {
          "name": "示例列名",
          "data_type": "VARCHAR(100)",
          "description": "说明字段业务含义、单位、枚举值和空值含义",
          "sensitive": false
        }
      ]
    }
  ],
  "queries": [
    {
      "id": "example_query",
      "description": "示例：按照开始时间和结束时间查询某项生产指标",
      "sql": "SELECT 示例列名 FROM 示例SCHEMA.示例表名 WHERE 时间列 >= ? AND 时间列 < ? FETCH FIRST 500 ROWS ONLY",
      "parameters": [
        {
          "name": "start_time",
          "description": "查询开始时间，格式 YYYY-MM-DD HH:MM:SS",
          "type": "string",
          "required": true
        },
        {
          "name": "end_time",
          "description": "查询结束时间，格式 YYYY-MM-DD HH:MM:SS",
          "type": "string",
          "required": true
        }
      ],
      "result_description": "返回生产指标明细；需要说明每列的单位和业务口径",
      "max_rows": 500
    }
  ]
}
